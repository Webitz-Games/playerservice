package main

func main() {
	/*	basePath := "/player"
		pService := playerservice.New(&config.Config{BasePath: basePath,},
			"player",
			nil)

		api.RegisterRoutes(basePath, "Player Service", pService)

		resp, _, err :=
			caller.Call(restful.DefaultContainer).
				To(gorequest.New().
					Get("/match2/apidocs/api.json").
					Type(restful.MIME_JSON).
					MakeRequest()).
				Execute()
		if err != nil {
			logrus.Fatal(err)
		}

		if resp.Code != http.StatusOK {
			logrus.Fatalf("received %v", resp.Code)
		}

		responseBody, err := ioutil.ReadAll(resp.Body)
		if err != nil {
			logrus.Fatal(err)
		}

		// nolint:gosec
		err = ioutil.WriteFile("api/api.gen.json", responseBody, 0644)
		if err != nil {
			logrus.Fatal(err)
		}*/
}
